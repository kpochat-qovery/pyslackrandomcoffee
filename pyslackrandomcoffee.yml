name: pyslackrandomcoffee
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - slack_sdk==3.36.0 # Send messages to slack channels and users
      - python-dotenv==1.1.1
